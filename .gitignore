node_modules
dist
.DS_Store
*.local
.vite

# Source material handed over for building the site. Kept out of the repo
# because Pages requires it to be public, and these files carry the full CV,
# student rosters and undergraduate employment records.
Website.zip
alhajri-labsite.zip
extracted/
labsite/

# Source figures, 17 MB of originals. scripts/optimize-figures.py turns these
# into the webp files under public/images/papers/, which are committed.
PublicationPictures/

# Leading slash anchors this to the repo root. Without it the pattern would
# also match public/images/pi/mohamed-alhajri.jpg, which the site needs.
/mohamed-alhajri.jpg
/Kenzy.jpg

# Roster of named undergraduates and their employers, including the ones with
# no destination recorded. The site publishes only the destinations it knows.
/Undergraduate_Students_List_Jobs.xlsx
