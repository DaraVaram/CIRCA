node_modules
dist
.DS_Store
*.local
.vite

# Source material handed over for building the site. Kept out of the repo
# because Pages requires it to be public, and these files carry the full CV,
# student rosters and undergraduate employment records.
Website.zip
extracted/
# Leading slash anchors this to the repo root. Without it the pattern also
# matches public/images/pi/mohamed-alhajri.jpg

# Source figures, 17 MB of originals. scripts/optimize-figures.py turns these
# into the 1400x467 webp files under public/images/papers/, which are committed.
PublicationPictures/, which the site needs.
/mohamed-alhajri.jpg

# Source figures, 17 MB of originals. scripts/optimize-figures.py turns these
# into the 1400x467 webp files under public/images/papers/, which are committed.
PublicationPictures/
